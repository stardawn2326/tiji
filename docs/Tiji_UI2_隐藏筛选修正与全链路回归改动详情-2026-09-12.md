# 题迹 UI 2.0 隐藏筛选修正与全链路回归改动详情

本轮依据 `Tiji_UI2_隐藏筛选修正与全链路回归方案_2026-09-12.md` 执行，继续使用 `codex/ui-2.0-paper-blue`，范围限定为清除知识点隐藏筛选状态和执行全链路回归。

远程仓库：`https://github.com/stardawn2326/tiji.git`

## 代码改动

### 知识点详情

- 删除“查看相关错题”跳转按钮和 `onOpenLibrary` 参数。
- 保留详情页内的“相关错题”列表；点击具体错题仍直接进入错题详情。

### 错题库与导航状态

- `LibraryScreen` 删除 `selectedKnowledgePointStableId`、`onSelectKnowledgePoint`、知识点实体/关联表参数和隐藏筛选计算。
- 错题库结果现在只受搜索、科目、掌握状态、难度和排序影响。
- `TijiApp`、`TijiNavGraph`、`TijiNavGraphState` 删除 `libraryKnowledgePointStableId` 和 `onLibraryKnowledgePoint` 状态链。
- 不恢复知识点 Chip、知识点筛选、笔记、错因或额外副文案；知识点实体、关联表、Repository、DAO 和数据库结构保持不变。

## 全链路回归

- 运行了当前仍有效的核心 UI、AI 保存、PDF、备份恢复、搜索、旧数据兼容和数据库迁移回归，共 `29` 个 instrumentation 测试，结果为 `OK (29 tests)`。
- 回归日志：`outputs/ui2-hidden-filter-full-regression.log`。
- 已确认错题库搜索、科目、掌握状态、四档难度、排序、批量复习、批量打印、批量删除和撤销链路通过。
- 已确认详情、复习手势/反馈/session 恢复、AI 结构化保存、PDF 练习版/答案版/原图选项、备份恢复、搜索和旧数据兼容链路通过。

## 构建与安装

- 执行 `compileDebugKotlin`、`compileDebugAndroidTestKotlin`、`lintDebug`、`assembleDebug`、`assembleDebugAndroidTest`，结果为 `BUILD SUCCESSFUL`。
- Lint：`0 errors, 5 warnings`；警告为既有依赖版本、系统栏弃用和原生库加载提示。
- 最终 APK 已安装到 `emulator-5556`。
- 交付 APK：`outputs/apk/tiji-ui2-paper-blue-correction-debug.apk`。
- APK SHA-256：`6C7F4BCA4BB9484E644F7260AF5440E6B205324A6C07AF4F0CB02C674C846940`。
- 自动化测试修改的模拟器数据已恢复，并完成 25 个文件的 SHA-256 校验。
- 按用户要求未进行截图或视觉检查。

## GitHub

- 本轮最终收口代码提交：`d03cb3c383aa27cb284d1574606cb2242e1d971f`。
- 目标分支：`codex/ui-2.0-paper-blue`。
- `main` 未修改。
