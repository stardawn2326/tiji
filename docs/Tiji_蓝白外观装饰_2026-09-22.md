# 蓝白外观装饰

在 bb7682f 的简洁布局上增加四类纯视觉细节：

- 一级标题下方加入短蓝线与淡蓝圆点，五个页面保持相同位置。
- 掌握概览增加低透明度同心圆轮廓、局部圆形底纹和角落点阵。
- 分区标题增加淡蓝色细分隔线。
- 首页学科图标底座增加角落弧形色块和小圆点。

全部使用主题色与 Compose drawBehind 绘制，适配浅深主题；装饰不参与布局、不接收点击、不产生朗读节点。未增加介绍卡片、宣传文字、图片依赖或循环动画，功能、标题对齐和操作区域不变。

验证范围：构建、安装和启动；本轮为纯绘制修改，不重复运行业务回归，不代替用户视觉验收。

结果：assembleDebug 构建成功；已安装至现有 API30 模拟器，安装返回 Success，MainActivity 启动成功。构建日志：outputs/ui-decoration-build.log。
