# Tiji UI 2.0 当前验收与 PR 集成改动详情

日期：2026-09-12  
仓库：[stardawn2326/tiji](https://github.com/stardawn2326/tiji)  
依据方案：`Tiji_UI2_当前验收与下一步PR集成方案_2026-09-12.md`

## 交付范围

本轮按方案完成独立 UI PR 集成、最终 APK 安装和交付记录。UI 2.0 代码与隐藏筛选修正保持冻结，本轮没有新增页面、业务功能、数据库结构或 AI/PDF/复习算法改动。

## PR

- PR：[\#8 refactor(ui): complete paper-blue UI 2.0](https://github.com/stardawn2326/tiji/pull/8)
- base：`codex/v1.4f-ai-solve-core`
- head：`codex/ui-2.0-paper-blue`
- 创建时 head：`c6ba466503a384875cc7ad7bcc6011124997ab51`
- 当前 head（含本交付文档）：`b697f9741e0db5a17e318e7cbe75873dc8103dbe`
- 创建时 base：`eef00b152a12efa5f7ac50d64a4c5c1f3e64d762`
- 状态：OPEN，未合并；没有向 `main` 直接提交 UI PR，也没有执行自动合并。

这个 base/head 关系使 UI PR 只包含 UI 2.0 相对于已验收核心基线的差异，避免把 PR #7 的核心改动重复带入 UI PR。

## 已纳入的代码改动

- 建立统一蓝白纸感 Design System，覆盖颜色、排版、尺寸、形状、动效、按钮、卡片、字段、反馈、弹窗、选择器和页面布局组件。
- 完成首页、错题库、录题、详情、复习、AI、设置现有页面迁移。
- 首页各科错题改为纵向长条结构。
- 难度统一为四档，保留旧数据值 `5` 并显示为“极难”。
- 移除错题主流程中的知识点、笔记、错因可见 UI。
- 移除错题库知识点入口、知识点筛选控件及 `selectedKnowledgePointStableId` 等隐藏筛选状态链。
- 保留知识点详情页内的相关错题列表及单题详情进入能力。
- 保留现有数据兼容和核心业务语义。

## 本地验证

### 构建、Lint 和 APK

执行任务：

```text
:app:compileDebugKotlin
:app:compileDebugAndroidTestKotlin
:app:lintDebug
:app:assembleDebug
:app:assembleDebugAndroidTest
```

结果：

```text
BUILD SUCCESSFUL
Lint: 0 errors, 5 warnings
```

构建日志：[outputs/ui2-hidden-filter-final-build.log](../outputs/ui2-hidden-filter-final-build.log)

APK：`outputs/apk/tiji-ui2-paper-blue-correction-debug.apk`  
SHA-256：`6C7F4BCA4BB9484E644F7260AF5440E6B205324A6C07AF4F0CB02C674C846940`

### 核心 instrumentation 回归

本地执行 17 个有效回归类，共 29 个测试，覆盖错题库筛选、批量复习/打印/删除、详情、复习手势与 session 恢复、AI 结构化保存、PDF、备份恢复、搜索、旧数据兼容、数据库迁移和竞态场景。

结果：

```text
OK (29 tests)
```

日志：[outputs/ui2-hidden-filter-full-regression.log](../outputs/ui2-hidden-filter-full-regression.log)

### 模拟器安装

- 设备：`emulator-5556`
- 包名：`com.tiji.mistakes`
- 安装结果：`adb install -r` 返回 `Success`
- 设备包路径：`/data/app/~~uJ_dhSgL2vw5EWCP2Odm5Q==/com.tiji.mistakes-yPRlblOlTkRoQmFmOjgb5Q==/base.apk`
- `versionCode=114`，`targetSdk=35`
- 安装后已执行既有设备快照恢复并校验，恢复文件数：25

本轮遵循用户要求，仅完成结构与代码交付，没有进行视觉检查。

## GitHub Actions 结果

PR #8 最终 head 对应 workflow run：[Android verification run 34692691867](https://github.com/stardawn2326/tiji/actions/runs/34692691867)

| 检查 | 结果 |
| --- | --- |
| Compile, test, lint and package | PASS |
| Android instrumentation (API 35) | FAIL |

API 35 job 共运行 55 个测试，8 个失败。失败原因均已从日志核对：

1. `FocusedReviewRecreationTest` 的 4 个用例、`FocusedReviewUiTest`、`KnowledgeDetailAnalyticsTest`、`KnowledgeExplorerTest` 仍查找 `library_open_knowledge`。该入口已按本方案从错题库移除，因此这些测试与当前冻结 UI 契约不一致。
2. `LibraryFilterTest.librarySearchAndFiltersChangeResults` 使用 `onAllNodesWithText("未掌握")[1]`，API 35 语义树只暴露一个可交互节点；本地 API 30 核心回归通过，生产筛选逻辑没有出现回退。

因此当前 PR 在 GitHub 上显示 `UNSTABLE`。按照方案约束，本轮不恢复已删除入口、不新增 UI/业务范围，也不把过时测试失败误报为 UI 回退；待后续按仓库测试契约统一处理后再重新验证 CI。

## 当前验收边界

- UI 代码、隐藏筛选修正、四档难度和本地 29 个核心回归结果已保留。
- PR 已推送到正确仓库和正确分支关系，未合并到 `main`。
- GitHub 编译/lint/打包检查通过；API 35 instrumentation 失败原因已记录，当前不满足“全部 CI 通过”的可合并条件。
- 等待最终合并决定，不自动合并。
