# 六色主题

保留晴蓝（blue）为默认主题，新增松绿（green）、湖青（teal）、鸢紫（purple）、玫粉（rose）、琥珀（amber）。原有 blue 存储值与未知值回退规则保持兼容。

## 实现

- 修复 TijiTheme 原先未使用 palette 参数的问题，所选配色实际参与 Material ColorScheme。
- 原晴蓝浅深色方案保持不变；其他主题配套生成主色、按钮前景、选中容器、背景、卡片、边框、深色表面等颜色。
- 错误色和复习状态色继续保留独立语义，不随装饰主题混淆。
- 外观页改用可换行色点选项，六种颜色直接可见，继续使用现有主题偏好保存、备份与恢复流程；不新增存储字段。
- 路径：设置 → 外观 → 主题色。显示模式依旧支持浅色、深色与跟随系统。

## 验证

新增 ThemePaletteTest：遍历六种配色的浅深模式，共检查 60 组主要文字/背景对比度 ≥ 4.5:1；验证外观页能选中每种颜色及未知 key 回退默认主题。另运行现有设置导航测试。不将静态对比度与自动化点击视为人工视觉验收。

结果：构建成功，API30 上 3 项测试全部通过（包括 60 组对比度检查）；安装返回 Success，MainActivity 启动成功。APK：app/build/outputs/apk/debug/tiji-v1.0.0-debug.apk。SHA256：41354F81E0F0E0A2988920DF1E350AFE62DFBA2CA3D327A0A04FC501CE707CF6。本地日志：outputs/theme-palettes-api30.log。
