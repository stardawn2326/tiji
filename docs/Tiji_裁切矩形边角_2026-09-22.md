# 裁切矩形边角

将图片处理页裁切框四角的圆形控制点改为主题色 L 形直角标记，带白色描边，在深浅图片上均有轮廓。角标默认长 18dp，随小裁切框缩短以避免互相重叠。

仅更改 Canvas 绘制，四角、四边与框内拖动命中及裁切计算均未改动。本轮验证构建和安装，不重复运行未受影响的图片业务测试，视觉由用户验收。

验证结果：assembleDebug 成功；API30 安装返回 Success，MainActivity 启动成功。日志：outputs/crop-corners-build.log。
