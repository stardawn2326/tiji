# 题迹 UI/UX 交互修正（2026-09-22）

基线：ea4a864。沿用蓝白主题，仅调整呈现与交互连接，不修改数据、AI、筛选规则或复习调度。

## 本轮修复

- 首页知识点卡片此前仅呈现统计，没有点击回调。现在以已有 stableId 跳转原知识点详情页，保留详情关联错题与专项复习功能，并增加箭头提示。
- 筛选区将两组不等宽 FlowRow 改为两行等宽布局。知识点、掌握状态、难度、排序统一对齐；长选项文本可缩小分配空间并省略，点击后仍可在原筛选窗口查看和修改。移除无操作的装饰列表图标。
- 主/次按钮纵向内边距由 12dp 收至 8dp；标签恢复 Material 原生紧凑外观与最小触摸区。学科统计行最小高度 88→72dp，复习评分卡最小高度 88→64dp，同时将纵向内边距 16→8dp、文字间距 8→4dp，内容仍允许随字体增长。
- 主/次按钮加入 120ms、0.97 倍轻按反馈；标签选中颜色 120ms 渐变；首页知识点展开收起使用已有 180ms 动效时长。使用 Compose 内置可中断动画，跟随系统动画时长设置；没有引入新动效依赖。保留原导航切页过渡。

## 验证边界

新增 HomeKnowledgeNavigationTest 验证展开学科后，点击知识点发出正确 stableId。导航接线复用已有知识点详情路由。继续运行 API30 导航、筛选、批量操作、复习和大字体用例。未执行人工截图审美验收。

## 最终结果

- 第一轮：新增首页知识点点击、主导航、筛选、批量操作、四档复习、大字体等 10 项 API30 测试全部通过。
- 收紧评分卡内边距后：复习与大字体 5 项针对性复验全部通过；最终 assembleDebug 与 connectedDebugAndroidTest 均 BUILD SUCCESSFUL。
- 本轮未重跑业务单元测试；生产修改均为 UI 文件。
- adb install -r 返回 Success，已启动 com.tiji.mistakes/.MainActivity。设备沿用 emulator-5554 / tiji-api30。
- APK：app/build/outputs/apk/debug/tiji-v1.0.0-debug.apk。
- SHA256：0190F54A0681C2EA9EF6A2F7D45242A87A247DF6C8BE8ED1A1FB33F2503B99BF。
- 本地日志：outputs/uiux-polish-api30.log、outputs/uiux-polish-final.log；第一轮 XML 留存 outputs/uiux-polish-api30-10.xml。
- 分支：codex/blue-white-uiux-20260921，目标仓库 stardawn2326/tiji。

